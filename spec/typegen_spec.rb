# frozen_string_literal: true

RSpec.describe Typegen do
  it "has a version number" do
    expect(Typegen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
